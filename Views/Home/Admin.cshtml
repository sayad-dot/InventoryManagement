@{
    ViewData["Title"] = "Admin Area";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-danger text-white">
                    <h4 class="card-title mb-0">
                        <i class="fas fa-lock me-2"></i>Admin Area
                    </h4>
                </div>
                <div class="card-body text-center">
                    <i class="fas fa-shield-alt fa-4x text-danger mb-3"></i>
                    <h5>Welcome to the Admin Area</h5>
                    <p class="text-muted">This area is restricted to administrators only.</p>
                    <p>You have special privileges to manage users and system settings.</p>
                    <a asp-action="Index" asp-controller="Home" class="btn btn-primary">Back to Home</a>
                </div>
            </div>
        </div>
    </div>
</div>